---
name: immich

services:
  immich-server:
    image: ghcr.io/immich-app/immich-server:v2.5.6@sha256:aa163d2e1cc2b16a9515dd1fef901e6f5231befad7024f093d7be1f2da14341a
    container_name: immich_server
    depends_on:
      - redis
      - database
    # extends:
    #   file: hwaccel.transcoding.yml
    #   service: cpu # set to one of [nvenc, quicksync, rkmpp, vaapi, vaapi-wsl] for accelerated transcoding
    env_file:
      - immich.env
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - upload:/usr/src/app/upload
      - nsfw:/mnt/media/nsfw:ro
    labels:
      - "de.panzer1119.docker.volume.default.cifs.host=op://Docker/Immich/CIFS/Host"
      - "de.panzer1119.docker.volume.default.cifs.username=op://Docker/Immich/CIFS/Username"
      - "de.panzer1119.docker.volume.default.cifs.password=op://Docker/Immich/CIFS/Password"
      - "de.panzer1119.docker.volume.immich_upload.cifs.share=op://Docker/Immich/CIFS/Share_Upload"
      - "de.panzer1119.docker.volume.immich_nsfw.cifs.share=op://Docker/Immich/CIFS/Share_NSFW"
    ports:
      - '2283:2283'
    healthcheck:
      disable: false
    restart: unless-stopped
    extends:
      file: ../../common/docker-compose.yml
      service: logging-gelf

  immich-machine-learning:
    # For hardware acceleration, add one of -[armnn, cuda, rocm, openvino, rknn] to the image tag.
    # Example tag: ${IMMICH_VERSION:-release}-cuda
    image: ghcr.io/immich-app/immich-machine-learning:v2.5.6@sha256:b213fa3c82d27a21a299c46ffbb38a091f18384db1ad67d409a3b34fe0fce556
    container_name: immich_machine_learning
    # extends: # uncomment this section for hardware acceleration - see https://immich.app/docs/features/ml-hardware-acceleration
    #   file: hwaccel.ml.yml
    #   service: cpu # set to one of [armnn, cuda, rocm, openvino, openvino-wsl, rknn] for accelerated inference - use the `-wsl` version for WSL2 where applicable
    env_file:
      - immich.env
    volumes:
      - model-cache:/cache
    healthcheck:
      disable: false
    restart: unless-stopped
    extends:
      file: ../../common/docker-compose.yml
      service: logging-gelf

  redis:
    image: docker.io/redis:6.2-alpine@sha256:46884be93652d02a96a176ccf173d1040bef365c5706aa7b6a1931caec8bfeef
    container_name: immich_redis
    healthcheck:
      test: redis-cli ping || exit 1
    restart: unless-stopped
    extends:
      file: ../../common/docker-compose.yml
      service: logging-gelf

  database:
    image: ghcr.io/immich-app/postgres:14-vectorchord0.3.0-pgvectors0.2.0@sha256:c570d9e1c2494f65d2a0a379a7f6df66e8441964254a30aa62cc58e8ebf1dee0
    container_name: immich_postgres
    env_file:
      - database.env
    volumes:
      - /docker/data/immich/postgres:/var/lib/postgresql/data
    restart: unless-stopped
    extends:
      file: ../../common/docker-compose.yml
      service: logging-gelf

  power-tools:
    image: ghcr.io/varun-raj/immich-power-tools:0.19.1@sha256:8e7a25ed0e99c9c323012e306fde6c97a1004765742c8802ad03c95503679e77
    container_name: immich_power_tools
    depends_on:
      - immich-server
      - database
    env_file:
      - power-tools.env
    ports:
      - "8132:3000"
    healthcheck:
      test: [ "CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/health" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    restart: unless-stopped
    extends:
      file: ../../common/docker-compose.yml
      service: logging-gelf

volumes:
  model-cache:
  upload:
    name: immich_upload
    # Because this is a cifs volume, and it needs credentials, it is created outside of this file
    external: true
  nsfw:
    name: immich_nsfw
    # Because this is a cifs volume, and it needs credentials, it is created outside of this file
    external: true
