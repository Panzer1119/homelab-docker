---
name: apprise

services:
  apprise:
    image: caronc/apprise:1.5@sha256:43673bf62faa88148298d52664b37b4860189555db0b251b54d79a3ef3a173cf
    container_name: apprise
    hostname: apprise
    environment:
      - TZ=Europe/Berlin
      - PGID=1000
      - PUID=1000
      - APPRISE_DEFAULT_THEME=dark
      - APPRISE_STATEFUL_MODE=simple
      - APPRISE_CONFIG_LOCK=yes
      - APPRISE_WORKER_COUNT=4
    volumes:
      - "/etc/localtime:/etc/localtime:ro"
      - /docker/config/apprise:/config
      - "${PWD}/config/pushover.yml:/config/pushover.yml:ro"
    ports:
      - "8116:8000"
    network_mode: bridge
    restart: always
    extends:
      file: ../../common/docker-compose.yml
      service: logging-gelf
