---
services:
  kometa:
    image: kometateam/kometa@sha256:4d7664af7a94d76b2f07ce0a9dc0ee88e52a952bbe6b27cef6cfd4c893f9f110
    container_name: kometa
    hostname: kometa
    env_file:
      - kometa.env
    volumes:
      - /docker/config/kometa:/config
      - "${PWD}/config/config.yml:/config/config.yml" #FIXME "PWD" might not work in all environments
    network_mode: bridge
    restart: no
    extends:
      file: ../../common/docker-compose.yml
      service: logging-gelf
